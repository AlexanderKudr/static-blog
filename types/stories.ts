export type StoryPage = {
  params: {
    slug: string;
  };
};
export type StoriesMetadata = {
  title: string;
  author: string;
  preview: string;
  slug: string;
  key: string;
  imgSrc: string;
  link: string;
  authorLink: string;
};
